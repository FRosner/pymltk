sudo: false
language: python
virtualenv:
    system_site_packages: true
python:
  - "2.7"
install: "pip install -U -r requirements.txt -r test-requirements.txt"
script: python setup.py test --addopts "tests --cov pyscaffold --cov-report term-missing" --verbose
